using UnityEngine;


public class DemonBattlePointHandler : MonoBehaviour
{
    /*
    // Singleton
    DemonBattlePointHandler Instance;
]
    void Awake()
    {
        if(Instance != null && Instance = this)
        {
            Destroy(gameObject);
            return;
        }

        Instance = this;
        DontDestroyOnLoad(gameObject);
    }
    */
    void Start()
    {
        
    }


    void Update()
    {
        
    }
}
